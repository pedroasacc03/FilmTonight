// Brand copy that should only ever need a one-line swap - per the Brand
// Foundation Brief, TAGLINE is deliberately its own exported constant
// (rather than typed inline wherever it's shown) so changing it later
// doesn't mean hunting through JSX across multiple files.
export const TAGLINE = "Try FilmTonight.";
